import Monolithic102.Catalog

namespace Monolithic102.ME032

def spec : EquationSpec := catalog[31]!

/- Canonical source expression:
lim(t→∞)||X(t; X\* + δX) − X\*|| = 0

Descriptor:
Tests whether the attractor survives disturbance and returns to its stable state.
-/

end Monolithic102.ME032
