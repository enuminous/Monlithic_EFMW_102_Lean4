import Monolithic102.Catalog

namespace Monolithic102.ME037

def spec : EquationSpec := catalog[36]!

/- Canonical source expression:
∂O/∂t = ΔO + α(∇O·∇S) + β(∇S·∇O) − (O − O³) − Π(O − O₀) + λV₁₃

Descriptor:
Observer-field counterpart to the S-field equation.
-/

end Monolithic102.ME037
