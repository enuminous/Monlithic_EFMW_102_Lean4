import Monolithic102.Catalog

namespace Monolithic102.ME046

def spec : EquationSpec := catalog[45]!

/- Canonical source expression:
C\_φ[X] = A(φX) + (φ − 1)[X − A(φX)]

Descriptor:
Golden-ratio-weighted local averaging plus residual restoration; A denotes a neighborhood averaging operator.
-/

end Monolithic102.ME046
