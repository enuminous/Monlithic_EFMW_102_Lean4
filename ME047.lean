import Monolithic102.Catalog

namespace Monolithic102.ME047

def spec : EquationSpec := catalog[46]!

/- Canonical source expression:
C = 1 − ||x − m|| / (||x|| + ||m|| + ε)

Descriptor:
Bounded similarity between system state and recursively reconstructed state.
-/

end Monolithic102.ME047
